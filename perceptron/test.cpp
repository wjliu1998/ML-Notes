#include<iostream>
#include "pri_perceptron.h"

using std::cout;
using std::endl;
int main(){
  double w[2] = {0.0, 0.0};
  Pri_perceptron perceptron = Pri_perceptron(1.0, w, 0.0);
  perceptron.getAnswer();
  return 0;
}  double w[2] = {0.0, 0.0};
  Pri_perceptron perceptron = Pri_perceptron(1.0, w, 0.0);
  perceptron.getAnswer();
  return 0;

